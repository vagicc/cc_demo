[package]
name = "cc_define"
version = "0.1.0"
edition = "2021"

# 编译 C 语言库时自定义设置
build = "build.rs"
[build-dependencies]
cc = "1"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
